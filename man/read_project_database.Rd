% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_project_database.R
\name{read_project_database}
\alias{read_project_database}
\title{Read a ShuttleboxR project-results database}
\usage{
read_project_database(file, standardise_names = TRUE)
}
\arguments{
\item{file}{Path to a project-results \code{.csv} file.}

\item{standardise_names}{Logical. If \code{TRUE} (the default), legacy names such
as \code{study_ID}, \code{distance}, and \code{shuttles} are converted to the current names
\code{fileID}, \code{tot_distance}, and \code{nr_shuttles}.}
}
\value{
A data frame containing one row per trial or individual.
}
\description{
Imports a comma-separated project-results table and standardises common
column names used by older ShuttleboxR versions. This makes existing project
databases compatible with the current project-level plotting and analysis
functions.
}
\details{
The following legacy names are recognised: \code{study_ID}, \code{distance},
\code{shuttles}, \code{pref_range}, \code{time_near_max}, \code{time_near_min}, and
\code{time_near_limits}. The original values are retained; only the column names
are changed. If no identifier column is available but \code{ID} is present, a
character \code{fileID} column is added.

Project databases store summary values and therefore cannot be used to
calculate a new \code{Tbreadth} value retrospectively. Thermal breadth must be
calculated from the underlying temperature observations using
\code{calc_project_results()}.
}
\examples{
example_file <- system.file(
  "extdata",
  "project_database_example.csv",
  package = "ShuttleboxR"
)

project_data <- read_project_database(example_file)
head(project_data)
}
\seealso{
\code{\link[=calc_project_results]{calc_project_results()}}, \code{\link[=plot_histograms]{plot_histograms()}}, \code{\link[=pca]{pca()}}
}
