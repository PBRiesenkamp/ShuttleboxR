% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_Tpercentile_range.R
\name{calc_Tpercentile_range}
\alias{calc_Tpercentile_range}
\title{Calculate a percentile-based thermal range}
\usage{
calc_Tpercentile_range(
  data,
  percentiles = c(0.25, 0.75),
  exclude_start_minutes = 0,
  exclude_end_minutes = 0,
  exclude_acclimation = FALSE,
  print_results = TRUE,
  exclude_gravitation = FALSE,
  gravitation_time = NULL
)
}
\arguments{
\item{data}{An organised shuttle-box data frame containing \code{core_T}.}

\item{percentiles}{Lower and upper percentiles. Default is \code{c(0.25, 0.75)}.}

\item{exclude_start_minutes}{Minutes omitted from the start of the selected period. Default is 0.}

\item{exclude_end_minutes}{Minutes omitted from the end of the recording. Default is 0.}

\item{exclude_acclimation}{Logical. Use only the dynamic period. Default is \code{FALSE}.}

\item{print_results}{Logical. Print the percentile boundaries and their difference. Default is \code{TRUE}.}

\item{exclude_gravitation}{Logical. Exclude the transitional gravitation period. Default is \code{FALSE} for backwards compatibility.}

\item{gravitation_time}{Advanced optional override in hours. Most users can leave this unset; when \code{exclude_gravitation = TRUE}, gravitation is estimated automatically.}
}
\value{
A single non-negative percentile range in degrees Celsius.
}
\description{
Calculates the difference between two percentiles of the selected core-temperature distribution. With the default 25th and 75th percentiles, this is the interquartile range (IQR): the width of the central 50 percent of experienced temperatures.
}
\details{
This metric complements \code{calc_Tbreadth()}. \code{Tpercentile_range} describes the width between two explicit percentile boundaries, whereas \code{Tbreadth} uses the complete distribution and averages all pairwise temperature differences. It also differs from \code{Tpref_range}, which is calculated from the avoidance percentiles selected in \code{calc_Tavoid()}.

When \code{exclude_gravitation = TRUE}, gravitation is estimated automatically unless \code{gravitation_time} is supplied as an advanced override.
}
\examples{
\dontrun{
fish <- read_shuttlesoft(file.choose())
calc_Tpercentile_range(fish, exclude_gravitation = TRUE)
calc_Tpercentile_range(
  fish,
  percentiles = c(0.10, 0.90),
  exclude_gravitation = TRUE
)
}
}
\seealso{
\code{calc_Tbreadth()}, \code{calc_Tavoid()}, \code{plot_coreT_histogram()}
}
